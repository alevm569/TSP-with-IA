def find_best_route_v3(_distances: np.ndarray) -> tuple[int, ...]:
    """Improved version of `find_best_route_v2` using a hybrid approach."""

    # Implement a hybrid approach that combines different heuristics or strategies.
    # For example, you could use a combination of nearest neighbor and 2-opt heuristics.

    # Perform local search on the initial route to find the best local optimum.

    return tuple(best_route)
