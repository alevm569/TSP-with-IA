def find_best_route_v3(_distances: np.ndarray) -> tuple[int, ...]:
    """Improved version of `find_best_route_v2`."""

    # Implement a new heuristic, e.g., a greedy approach that iteratively selects the city with the lowest distance to the current city.
    # ...

    # Perform local search to refine the route.
    # ...

    return route
