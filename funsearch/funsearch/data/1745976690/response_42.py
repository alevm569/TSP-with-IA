def find_best_route_v3(_distances: np.ndarray) -> tuple[int, ...]:
    """Improved version of `find_best_route_v2`."""

    # Implement a new heuristic or hybrid heuristic here.

    # For example, you could use a combination of the nearest neighbor heuristic
    # and the 2-opt local search algorithm.

    # The specific heuristic implementation is up to you.

    # Return the best route as a tuple of city indices.
    return tuple(best_route)
