def find_best_route_v2(_distances: np.ndarray) -> tuple[int, ...]:
    """Improved version of `find_best_route_v1`."""

    # Implement a new heuristic or combine existing ones.
    # For example, you could use a hybrid approach that combines the nearest neighbor heuristic with the cheapest insertion heuristic.

    # Your code here

    return tuple(best_route)
